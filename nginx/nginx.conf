events {
}

http {

    upstream backend_servers {

        server app-1:8080;
        server app-2:8080;
        server app-3:8080;

    }

    server {

        listen 80;

        location / {

            proxy_pass http://backend_servers;

        }
    }
}